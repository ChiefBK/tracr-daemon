package strategies

import (
	"goku-bot"
	"log"
)

func Strategy1(pair, exchange string, store goku_bot.Store) (actionQueue goku_bot.ActionQueue, err error) {
	log.Println("Running Strategy 1")

	return
}
